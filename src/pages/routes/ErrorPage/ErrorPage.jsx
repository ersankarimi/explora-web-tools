import React from 'react'
import { PageNotFound } from '@features/ui'

/**
 * * Returns a component that renders the content of the section.
 * @returns {JSX.Element} PageNotFound - JSX Element of the Page Not Found Routes .
 */
const ErrorPage = () => {
    return <PageNotFound />
}

export default ErrorPage
